# Test Extension Locally

Run these commands to test the extension:

1. Build in watch mode:
```bash
npm run watch
```

2. Press F5 in VS Code to launch Extension Development Host

3. In the new window, run "Fetch Claude Usage Now" from Command Palette (Ctrl+Shift+P)

4. Check "Claude Usage - Token Monitor" output channel for logs
